Read the file /backend/server.py carefully before making any changes.

## Goal
Implement a secure refresh token system with sliding expiry, replacing the current single-token authentication.

## Changes to server.py

### 1. New imports and constants (add near the top with other imports/constants)
```python
from datetime import datetime, timedelta
import secrets

ACCESS_TOKEN_EXPIRE_MINUTES = 15
REFRESH_TOKEN_EXPIRE_DAYS_STUDENT = 60
REFRESH_TOKEN_EXPIRE_DAYS_LANDLORD = 30
MAX_SESSIONS_PER_USER = 3
```

### 2. New MongoDB collection
Add a reference to a new collection after the existing collection references:
```python
db.refresh_tokens  # for session management
```

### 3. New helper functions (add after existing token helpers)

#### create_refresh_token()
```python
def create_refresh_token(user_id: str, role: str) -> str:
    token = secrets.token_urlsafe(64)
    expire_days = REFRESH_TOKEN_EXPIRE_DAYS_STUDENT if role == 'student' else REFRESH_TOKEN_EXPIRE_DAYS_LANDLORD
    expires_at = datetime.utcnow() + timedelta(days=expire_days)
    return token, expires_at
```

#### store_refresh_token()
Async function that:
1. Counts existing sessions for this user_id in db.refresh_tokens
2. If count >= MAX_SESSIONS_PER_USER (3), delete the oldest session (sort by created_at ascending, delete first)
3. Inserts new document: { user_id, token, role, expires_at, created_at: datetime.utcnow() }

#### verify_refresh_token()
Async function that:
1. Finds document in db.refresh_tokens where token matches AND expires_at > datetime.utcnow()
2. If not found: return None
3. If found: apply sliding expiry — update expires_at to datetime.utcnow() + timedelta(days=expire_days based on role)
4. Return the document

### 4. Update the /auth/login endpoint (currently at line 425)
After creating the access token on line 444, also:
1. Create a refresh token using create_refresh_token(user['id'], user['role'])
2. Call store_refresh_token() to save it
3. Add 'refresh_token' to the return dict alongside 'token' and 'user'

The return dict should become:
```python
return {
    'token': token,
    'refresh_token': refresh_token,
    'user': { ...existing user fields... }
}
```

### 5. New endpoint: POST /auth/refresh
Add this BEFORE any parameterized routes. It should:
1. Accept JSON body with { "refresh_token": string }
2. Call verify_refresh_token() — if None, raise HTTPException(status_code=401, detail='Ongeldige of verlopen sessie')
3. Fetch the user from db.users by user_id from the token document
4. Create a new access token: create_token(user['id'], user['role'])
5. Return { 'token': new_access_token, 'user': { ...same fields as login... } }

### 6. New endpoint: POST /auth/logout
Add after /auth/refresh. It should:
1. Accept JSON body with { "refresh_token": string }
2. Delete the matching document from db.refresh_tokens
3. Return { 'message': 'Uitgelogd' }

### 7. Create MongoDB index (add in startup or after collection references)
```python
await db.refresh_tokens.create_index("expires_at", expireAfterSeconds=0)
```
This auto-deletes expired tokens from MongoDB.

## Important rules
- Do NOT change any existing endpoints other than /auth/login
- Do NOT change the create_token() function or JWT structure
- Keep all existing imports
- Maintain FastAPI route ordering: specific routes before parameterized routes
- All status values remain lowercase