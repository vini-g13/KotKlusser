You are working on KotKlusser. Fix a mobile horizontal overflow issue in `frontend/src/pages/LandlordDashboard.jsx`.

## PROBLEM
The entire main content area (tiles, filters, ticket list) overflows horizontally on mobile — content is cut off on the right side. The page is wider than the mobile viewport.

## ROOT CAUSE
The main content wrapper uses `lg:ml-64` to offset for the desktop sidebar, but on mobile this combined with inner padding/widths causes horizontal overflow.

## FIX

### Step 1 — Add overflow-x-hidden to the root div
Find the outermost div of the component:
```jsx
<div className="min-h-screen bg-[#0B0A14] flex">
```
Replace with:
```jsx
<div className="min-h-screen bg-[#0B0A14] flex overflow-x-hidden">
```

### Step 2 — Fix the main content area
Find:
```jsx
<main className="flex-1 lg:ml-64">
```
Replace with:
```jsx
<main className="flex-1 lg:ml-64 min-w-0">
```

### Step 3 — Fix the inner content container
Find the div with `pt-20 lg:pt-8` (the padding-top wrapper inside main):
```jsx
<div className="pt-20 lg:pt-8 pb-8 px-4 sm:px-6 lg:px-8">
```
Replace with:
```jsx
<div className="pt-20 lg:pt-8 pb-8 px-4 sm:px-6 lg:px-8 w-full min-w-0">
```

### Step 4 — Fix the max-width inner container
Find:
```jsx
<div className="max-w-6xl mx-auto">
```
Replace with:
```jsx
<div className="max-w-6xl mx-auto w-full min-w-0">
```

### Step 5 — Fix the tiles grid
Find the grid div that renders the stat tiles (contains the tile buttons). Ensure it has `w-full` and no possibility of overflowing. The grid should be:
```jsx
className="grid grid-cols-2 gap-3 mb-6 w-full"
```
Keep all existing motion/animation props, only fix the className.

### Step 6 — Fix filter row
Find the div containing the filter dropdowns (Alle status, Alle categorieën, Alle panden selects). Add `flex-wrap` and `w-full` if not already present:
```jsx
className="flex flex-wrap gap-2 w-full"
```

Do not change any visual styling, colors, or desktop layout. Only add `min-w-0`, `w-full`, `overflow-x-hidden` and `flex-wrap` where needed to prevent horizontal overflow on mobile.